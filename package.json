{
  "name": "jarvis-ai-backend",
  "version": "1.0.0",
  "description": "Secure proxy between JARVIS AI (Android) and the Anthropic API. Holds the real API key so it is never shipped inside the APK.",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
